# BEGIN DAW — pipeline runtime, NOT committed (managed by DAW)
#   .daw-state.json  = which phase you are in; yours, not the repo's
#   .daw-paused/     = paused tickets
#   .daw-sessions/   = live session markers
#   .daw-journal.jsonl = transitions that landed; outlives the state file
.daw-state.json
.daw-paused/
.daw-sessions/
.daw-journal.jsonl
# END DAW

# Python bytecode (scripts de .daw/scripts/)
__pycache__/
*.pyc

# .NET (Block 1 — FEAT-001a)
bin/
obj/
*.user
appsettings.*.local.json

# Docker (Block 2 — FEAT-002): credenciales locales de desarrollo, nunca versionadas
# (mitigación riesgo #2 del threat model). .env.example SÍ se commitea.
.env
